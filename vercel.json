{
  "installCommand": "cd front && pnpm install",
  "buildCommand": "cd front && pnpm run build",
  "framework": "nextjs",
  "rootDirectory": "front",
  "regions": ["iad1"],
  "functions": {
    "front/app/**/*.{js,ts,tsx}": {
      "maxDuration": 30
    }
  }
}