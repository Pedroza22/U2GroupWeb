{
  "installCommand": "npm install --save-dev typescript@5.9.2 @types/react@19.1.15 @types/node@22.18.7 @types/react-dom@19.1.9 --legacy-peer-deps && npm install --legacy-peer-deps && npm list typescript || exit 1",
  "buildCommand": "npm list typescript && npm run build",
  "framework": "nextjs",
  "regions": ["iad1"],
  "functions": {
    "app/**/*.{js,ts,tsx}": {
      "maxDuration": 60
    }
  },
  "env": {
    "NPM_CONFIG_PACKAGE_LOCK": "true",
    "NEXT_TELEMETRY_DISABLED": "1",
    "NODE_OPTIONS": "--max-old-space-size=4096",
    "NEXT_FORCE_INSTALL_DEPS": "true",
    "NEXT_PRIVATE_SKIP_SETUP": "true"
  }
}